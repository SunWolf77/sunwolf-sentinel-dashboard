# Added better error handling and note about intermittent feeds on free hosting
st.info('Some external feeds may show temporary warnings on first load/free tier — click Refresh All. Works reliably after cache.')